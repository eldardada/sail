const basket = document.querySelector('.basket');

if(basket) {
    const basketModels = document.querySelector('.basket-models');
    btnClickAnimation(basketModels, '.basket-bgmenu-count', '.btn_count', '.btn_count_inc');
}
